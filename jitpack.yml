jdk:
  - openjdk17
before_install:
  - if [ -f ./scripts/prepareJitpackEnvironment.sh ]; then ./scripts/prepareJitpackEnvironment.sh; fi



install:
  - ./gradlew clean assemble

script:
  - ./gradlew publishToMavenLocal